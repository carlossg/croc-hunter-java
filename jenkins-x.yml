buildPack: maven-quarkus
buildPackGitURL: https://github.com/carlossg/jenkins-x-kubernetes
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        options:
          containerOptions:
            resources:
              limits:
                cpu: 1
                memory: 2Gi
              requests:
                cpu: 1
                memory: 2Gi
        agent:
          image: docker.io/csanchez/builder-graalvm
    release:
      pipeline:
        options:
          containerOptions:
            resources:
              limits:
                cpu: 1
                memory: 2Gi
              requests:
                cpu: 1
                memory: 2Gi
        agent:
          image: docker.io/csanchez/builder-graalvm
